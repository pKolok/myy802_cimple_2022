program exams
{
	declare a,b,c,d,e;  
    
	function max(in x, in y)  
    {   
		if (x>y) 
           return(x); 
        else 
           return(y); 
    }  
 
	input(a);
	input(b);
	input(c);
	input(d);
	
	e:=max(in max(in a, in b), in max(in c, in d));
	
	print(e);
}.