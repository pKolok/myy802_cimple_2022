program test
{
	# declarations #
	declare x, y;
	declare i;
	
	function fun1 (in x, inout k)
	{
		declare i;
		
		x := x + 1;
		k := x;
		
		return(x)
	}
	
	while (a > b)
	{
		b := b + 1;
	};
	
	if (c > d)
		d := d + 1;
	else
		d := d - 1;;
	
	# x:= 2+1+(3*(3+2))-16/4*4; #
	
	input(y);
	
	k := 10 + fun1(in x, inout y);
	
	call assign(in a, inout b);
	
	return(x);
}.